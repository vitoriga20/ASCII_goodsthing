# CI-only OTIO validation dependency (Phase 48). The app never ships or
# requires Python — this is consumed solely by .github/workflows/otio-validation.yml
# to parse the golden .otio fixtures with the reference implementation.
#
# Pinned to the release the golden fixtures were verified against; bump
# deliberately alongside a fixture re-check. Keeping it in this file (rather
# than inline in the workflow) lets actions/setup-python key its pip cache on it.
opentimelineio==0.18.1
